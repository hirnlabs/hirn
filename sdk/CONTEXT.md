# SDK Context

The TypeScript SDK for building modular, offline-first tools that run in Hirn.

## Language

**SDK**:
The library allowing custom offline applets to communicate with the Rust Sync Core.
