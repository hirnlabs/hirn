# Homepage Context

The web dashboard and documentation portal for Hirn.

## Package Manager & Tooling

- Use **Bun** (`bun`) for dependency management and running scripts (`bun install`, `bun run dev`, `bun run build`, `bun run check`).

## Language

**Dashboard**:
The web interface presenting metrics and settings for the user's Hirn cluster.
