---
import EarlyAccessForm from "./EarlyAccessForm.svelte";

interface Props {
  class?: string;
  lang?: string;
}
const { class: className, lang = "en" } = Astro.props;
---

<EarlyAccessForm lang={lang} className={className} client:load />
